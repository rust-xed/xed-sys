language: rust
os:
    - linux
rust:
    - stable
    - beta
    - nightly
matrix:
    allow_failures:
        - rust: nightly

before_install:
    - sudo apt-get update
    - sudo apt-get install -y llvm-3.9-dev libclang-3.9-dev clang-3.9
